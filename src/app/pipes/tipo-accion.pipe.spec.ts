import { TipoAccionPipe } from './tipo-accion.pipe';

describe('TipoAccionPipe', () => {
  it('create an instance', () => {
    const pipe = new TipoAccionPipe();
    expect(pipe).toBeTruthy();
  });
});
