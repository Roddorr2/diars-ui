import { TipoMovimientoInventarioPipe } from './tipo-movimiento-inventario.pipe';

describe('TipoMovimientoInventarioPipe', () => {
  it('create an instance', () => {
    const pipe = new TipoMovimientoInventarioPipe();
    expect(pipe).toBeTruthy();
  });
});
