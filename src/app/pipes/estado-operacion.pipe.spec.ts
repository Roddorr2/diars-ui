import { EstadoOperacionPipe } from './estado-operacion.pipe';

describe('EstadoOperacionPipe', () => {
  it('create an instance', () => {
    const pipe = new EstadoOperacionPipe();
    expect(pipe).toBeTruthy();
  });
});
