import { TipoMovimientoAlmacenPipe } from './tipo-movimiento-almacen.pipe';

describe('TipoMovimientoAlmacenPipe', () => {
  it('create an instance', () => {
    const pipe = new TipoMovimientoAlmacenPipe();
    expect(pipe).toBeTruthy();
  });
});
